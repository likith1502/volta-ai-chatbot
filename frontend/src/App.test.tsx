import { describe, expect, it } from 'vitest'
import { App } from './App'

describe('Enterprise Admin Console App', () => {
  it('exports App component successfully', () => {
    expect(App).toBeDefined()
  })
})
